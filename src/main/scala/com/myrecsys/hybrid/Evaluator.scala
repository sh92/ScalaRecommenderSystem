package com.myrecsys.hybrid

import com.myrecsys.ALS.ALSRecSys.parseRating

class Evaluator {
  var algorithms: List[Algorithm] = Nil

//  def init(data, ranking): Unit = {
//    val ratings = spark.createDataFrame(data.map(parseRating))
//
//  }
//
//  def Evaluate(doTopN): Unit ={
//
//  }
}
